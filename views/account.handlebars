<section class="container">
    <section class="account">
        <a href="/account/{{id}}">
            <h3>{{account_type}}</h3>
            <p>{{balance}}</p>
        </a>
    </section>
</section>  
